# Backend API endpoint for the DALL·E proxy.
# Optional — if unset, the client falls back to:
#   development → http://localhost:8080/api/v1/dalle
#   production  → https://devswag.onrender.com/api/v1/dalle
# Override here when running the client against a different server.
VITE_BACKEND_URL=
